export interface LoadingDotsProps {
  productTitle?: string;
  wordsWidth?: number;
}

export interface DotProps {
  delay: string;
  dotSize?: number;
}
export interface WordsProps {
  wordsWidth?: number;
}
